<Window x:Class="SysQ.Microgestion.Frontend.Sales.Wpf.Views.FindItemView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:my="clr-namespace:Microsoft.Windows.Controls;assembly=WPFToolkit"
        Title="Consulta de Precios"
        WindowStyle="None"
        AllowsTransparency="True"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="{StaticResource blueBackground}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Búsqueda de Artículos:" Grid.Row="0" />
        <TextBox x:Name="FindItemText" Grid.Row="1" />

        <my:DataGrid Grid.Row="2" 
                     Height="250" Width="400" 
                     Name="FindItemGrid" />
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Agregar" IsDefault="True" Name="FindItemAddButton" />
            <Button Content="Volver" IsCancel="True" Name="FindItemBackButton" />
        </StackPanel>
        
    </Grid>
</Window>
